STATUS: ISSUES

## 问题 1
类型：格式
原文：`date: 2026-07-14
retrospect: "2025-07-13"   # 那年今日回顾：原事件日（媒体实名转发、热搜第一）`
<!-- [REVIEWER]: `scripts/calendar.js` 里那年今日回顾的紫色 ↺ 标记不是按 `retrospect` 字段
（原事件日+1年）单独计算日历格的，而是直接用 `post.date.format('YYMMDD')` 作 key
（见 calendar.js 第21-26行：`const key = post.date.format('YYMMDD'); ... if (post.retrospect)
{ if (!retroMap[key]) retroMap[key] = []; retroMap[key].push(...); return; }`）——也就是说
↺ 具体挂在哪一天的日历格，完全由 frontmatter `date:` 决定，与 `retrospect:` 的值无关。
本文 `retrospect: "2025-07-13"`（原事件——媒体实名转发、热搜第一——发生日），"那年今日"
对应的日历格应是一年后的同一天，即 2026-07-13；但 `date: 2026-07-14` 比这晚一天（取的是
本文最新事实性进展——"饭哲惜的木屋"发帖——的发生日，符合 template 对 date 字段的一般规则，
但与"那年今日回顾"想要对齐的锚点日期错开一天）。按当前代码，↺ 标记与"该日不显示挑战失败/
不打断 Day N 计数"的特殊处理会落在 2026年7月14日的日历格上，而不是 2026年7月13日（真正的
一周年纪念日）——这是本功能（2026-08-04 才实现，本文可能是首篇使用该字段的稿件，无既往
先例可循）"那年今日"这个名字所暗示的语义落空的具体表现。请用户裁定：(a) 把 `date:` 改为
2026-07-13 以对齐日历格（但这会偏离"date＝最新事实性进展发生日"的一般规则，因为发帖实际
发生在14日）；或 (b) 确认当前设计本就允许 date 与 retrospect+1年之间存在一两天误差，↺
标记落在"最新进展发生日"而非精确周年日是可接受的；或 (c) 调整 calendar.js，让 retroMap 的
key 改用 `retrospect` 字段计算出的周年日而非 `post.date`，使二者解耦。三条路径互斥，需要
用户决定后再由写手/工程侧落实，不属于写手可单方面判断的格式细节。 -->
处理：已修改（工程侧，草稿不变）——采纳路径 (c) 的变体：改 `scripts/calendar.js`，`retroMap` 的 key 由新增的 `retroKey()` 计算，取「原事件的月日 ＋ 本文 `date` 的年份」，与 `post.date` 解耦。本文 ↺ 因此落在 2026-07-13 格（真正的周年日），`date: 2026-07-14` 与 `retrospect: "2025-07-13"` 均按原样保留，草稿无需任何改动。

<!-- [USER]: 2026-08-05 裁定：(a)(b) 均不取——(a) 会破坏「date＝最新事实性进展发生日」的一般规则，(b) 会让 `retrospect` 字段沦为纯布尔开关、其值永不参与定位。取 (c)，但不用评审设想的「原事件日+1年」：那只对「去年今日」成立，原事件若是更早年份的，+1 年会落到不存在或不相干的月历上；改用「原事件月日 ＋ 本文 date 年份」，可支持任意年差，且原事件与本文同年时即等于字面的原事件日。日历只从 2026-01 起渲染，直接用 `retrospect` 的年份会落进不存在的月历，故不采用字面日期。2-29 撞非闰年收到当月最后一天。已 build 验证：↺ 落 7/13 格、颜色按本文档位取（本文 B 档＝橙），文章自身 date 那格不受影响、绿色 Day N 计数不打断。同日裁定 ↺ 由固定紫色改为按档位取色，并把 M 档文章接入日历（文章 date 那格日期号旁挂紫色加粗 M）。规则已写入 CLAUDE.md「Landing-page Calendar」节与 `source/_drafts/template.md` 的 frontmatter 说明。 -->

## 标签提案

无。
